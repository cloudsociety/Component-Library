import React from "react";
import { storiesOf } from "@storybook/react";
import {{pascalCase name}} from './';

import General from './{{pascalCase name}}.md';

let stories = storiesOf('{{type}}|{{pascalCase name}}', module);

stories.add('Default', () => (
  <{{pascalCase name}} />
), {
  notes: {General}
});
